# UI Design Spec — Modern Dark Dashboard

**Date:** 2026-06-01
**Scope:** Visual redesign of all client pages and components using inline styles. No new dependencies.

---

## 1. Design Direction

**Style:** Modern Dark Dashboard (GitHub-inspired)
**CSS approach:** Inline styles only — no CSS modules, no Tailwind, no external libraries.

---

## 2. Color Palette

| Role | Value |
|---|---|
| Page background | `#0f1117` |
| Surface (cards, table rows, panels) | `#161b22` |
| Border | `#21262d` |
| Text primary | `#e6edf3` |
| Text muted | `#8b949e` |
| Accent blue (links, symbols) | `#58a6ff` |
| Active nav underline | `#1f6feb` |
| Green (up tick, live status) | `#3fb950` |
| Red (down tick) | `#f85149` |
| Hover row background | `#1c2128` |
| Nav background | `#161b22` |
| Nav border bottom | `#21262d` |

---

## 3. Top Navigation Bar

Single horizontal bar at the top of every page.

**Layout:** `[◈ prep-for-work] [|] [Dashboard] [Watchlist] [Chart: SYMBOL?] [Settings]` ··· `[● LIVE / connecting / disconnected] [524/s]`

- Logo: `◈ prep-for-work`, color `#58a6ff`, font-weight 700, font-size 13px
- Divider: `1px solid #21262d`, height 16px
- Nav links: font-size 13px, color `#8b949e`, no text-decoration, padding `4px 0`
- Active link: color `#58a6ff`, borderBottom `2px solid #1f6feb`
- Chart link: only shown when `selectedSymbol !== null`
- WS status dot: 8px circle, green `#3fb950` / amber `#f0883e` / red `#f85149`
- ticks/s: font-size 12px, color `#8b949e`, font-family monospace
- Bar background: `#161b22`, borderBottom `1px solid #21262d`, padding `0 20px`, height 44px

---

## 4. Page Layouts

### 4.1 DashboardPage

Three KPI cards in a row + TopVolatileList below.

**KPI cards:**
- Container: `display:grid, gridTemplateColumns: repeat(3, 1fr), gap: 12px, padding: 20px`
- Card: background `#161b22`, border `1px solid #21262d`, border-radius 8px, padding 16px
- Card label: font-size 11px, color `#8b949e`, text-transform uppercase, letter-spacing 0.5px, margin-bottom 4px
- Card value: font-size 24px, font-weight 700, color `#e6edf3`, font-family monospace

**KPI card values:**
- ticks/s — value color `#e6edf3`
- symbols — value color `#e6edf3`
- WS status — shows `● connected` / `◌ connecting` / `✕ disconnected` with appropriate color

**TopVolatileList:**
- Wrapper: background `#161b22`, border `1px solid #21262d`, border-radius 8px, margin `0 20px 20px`, padding 16px
- Section title: font-size 12px, color `#8b949e`, text-transform uppercase, letter-spacing 0.5px, margin-bottom 12px
- Table: width 100%, border-collapse collapse
- Header row: font-size 11px, color `#8b949e`, border-bottom `1px solid #21262d`, padding `0 0 8px`
- Data row: font-size 13px, padding `6px 0`, border-bottom `1px solid #21262d`
- Symbol column: color `#58a6ff`, font-weight 600, font-family monospace
- Price column: color `#e6edf3`, font-family monospace, text-align right
- Delta column: green `#3fb950` / red `#f85149`, text-align right, font-family monospace

### 4.2 WatchlistPage

Full-height scrollable table.

- Page wrapper: background `#0f1117`, height `calc(100vh - 44px)`
- Page title: font-size 13px, color `#8b949e`, text-transform uppercase, letter-spacing 0.5px, padding `16px 20px 8px`
- Table wrapper: `overflowY: auto`, height `calc(100vh - 44px - 48px)`
- Table: width 100%, border-collapse collapse
- Sticky header: background `#0f1117`, borderBottom `1px solid #21262d`, position sticky, top 0, font-size 11px, color `#8b949e`, padding `8px 20px`, text-transform uppercase
- Row: padding `6px 20px`, border-bottom `1px solid #21262d`, cursor pointer
- Row hover: background `#1c2128`
- Symbol cell: color `#58a6ff`, font-weight 600, font-family monospace, font-size 13px, width 80px
- Price cell: color `#e6edf3`, font-family monospace, font-size 13px, text-align right
- Time cell: color `#8b949e`, font-size 11px, text-align right

### 4.3 ChartPage

Centered chart panel.

- Wrapper: padding 20px, background `#0f1117`, min-height `calc(100vh - 44px)`
- Header row: display flex, align-items baseline, gap 12px, margin-bottom 16px
- Symbol heading: font-size 20px, font-weight 700, color `#e6edf3`, font-family monospace
- Current price: font-size 24px, font-weight 700, color `#e6edf3`, font-family monospace
- Canvas: display block, width 100%, max-width 800px, height 240px, background `#161b22`, border `1px solid #21262d`, border-radius 8px, stroke color `#58a6ff`

### 4.4 SettingsPage

Form layout, max-width 480px.

- Wrapper: padding 24px, background `#0f1117`, min-height `calc(100vh - 44px)`
- Title: font-size 16px, font-weight 600, color `#e6edf3`, margin-bottom 24px
- Label text: font-size 13px, color `#8b949e`, margin-bottom 6px
- Value display: color `#e6edf3`, font-weight 600, font-family monospace
- Range input: width 100%, accent-color `#1f6feb`
- Apply button: background `#1f6feb`, color `#fff`, border none, border-radius 6px, padding `8px 20px`, font-size 13px, cursor pointer
- Apply button hover: background `#388bfd`

---

## 5. Global Body Styles

Applied in `index.html` `<style>` tag:

```css
body {
  margin: 0;
  background: #0f1117;
  color: #e6edf3;
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: 14px;
  -webkit-font-smoothing: antialiased;
}
* { box-sizing: border-box; }
```

---

## 6. Out of Scope

- Responsive / mobile layout
- Animations or transitions
- Light theme toggle (AppContext has theme state but not wired in this pass)
- Custom fonts or icon libraries
